FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npx prisma generate
RUN npm run build
RUN echo "=== After build ===" && ls -la dist/

RUN npm prune --production
RUN echo "=== After prune ===" && ls -la dist/

EXPOSE 3000

CMD ["node", "dist/main.js"]
